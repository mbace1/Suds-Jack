# Reference

Animation reference for the character. Nothing here is loaded by the game
except `jimbo.png`, which IS the character — he is blitted, not drawn.

| file | what it is |
| --- | --- |
| `conrad.png` | Conrad B. Hart's frames from Flashback (SNES), ripped by LuigiBlood, from The Spriters Resource. 1088×2928, a clean 32×48 grid, 61 rows, 761 frames, fourteen colours. The source of every move he has. |
| `jimbo.png` | the same sheet repainted as Jimbo — brown trousers, white tee, dark letterman jacket. Generated by `recolour.py`; do not hand-edit it, edit the script. |
| `pop.png` | the Prince from Prince of Persia, ripped by Skylar Welke (Welkup, 2003). 430×1112. His figure is 40px tall against Conrad's 38, so the two mix without scaling — this is where the sword comes from. |
| `recolour.py` | conrad.png → jimbo.png. Run it after changing the outfit. |

## What a repaint can and cannot do

Most of the outfit is one lookup, because the sheet is indexed art: five blues
of denim become browns, two magentas of undershirt become a white tee, the
jacket browns become a dark letterman.

**Hair** is not a lookup — it shares a colour with the jacket, `#633100` being
both. So it is split by REGION: inside the top eight rows of a frame that
colour is hair and stays brown, below it is jacket and goes dark.

**Sleeves** cannot be split at all. The four browns are *shading* — lit side,
shade side, fold, edge — laid across the whole garment rather than body versus
sleeve, so there is no set of pixels that means "sleeve". Grey sleeves need
them picked out by hand, frame by frame.

**Silhouette** is not colour. Baggier trousers are a different outline and no
repaint reaches them; that is a redraw of the sheet, or the Prince of Persia
frames, whose trousers already are baggy.

Delphine Software own Conrad; Broderbund own the Prince. The animation is
reference; the pixels here are theirs.
