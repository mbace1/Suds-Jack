// TOKO MIDORI GAMES — cross-game awareness.
import { GAMES } from '../../hub/games.js';
import { readPlayLog, favourites, setFavourite } from '../../hub/playlog.js';
const getRaw=k=>{try{return localStorage.getItem(k)}catch{return null}},parse=v=>{if(v==null)return null;try{return JSON.parse(v)}catch{return v}},label=g=>g.title||g.id,mins=s=>s<60?`${s}s`:`${Math.round(s/60)} min`;
const scoreOf=g=>{if(!g.score?.key)return null;const raw=parse(getRaw(g.score.key)),n=typeof raw==='number'?raw:parseFloat(raw);return Number.isFinite(n)&&n>0?n:null};
const fmt=(g,n)=>g.score?.fmt==='secs'?`${Number(n).toFixed(2)} sec`:`${Math.round(n)} pts`;
function runStats(events){const ends=events.filter(x=>['failure','death','gameover','runend'].includes(x.type)),causes={};for(const e of ends){const c=e.cause||e.killer||e.reason;if(c)causes[c]=(causes[c]||0)+1}return{ends,topCause:Object.entries(causes).sort((a,b)=>b[1]-a[1])[0]||null}}
export function snapshot(){const log=readPlayLog({limit:240}),fav=new Set(favourites());const games=GAMES.map(g=>{const events=log.filter(x=>x.game===g.id),sessions=events.filter(x=>x.type==='session'),ss=sessions.map(x=>Number(x.seconds)||0).filter(Boolean),rs=runStats(events),last=events.at(-1)||null;return{id:g.id,title:label(g),lineage:g.lineage||'',tags:g.tags||[],best:scoreOf(g),scoreFmt:g.score?.fmt||null,visits:events.filter(x=>x.type==='visit').length,runs:events.filter(x=>x.type==='run'||x.type==='start').length,failures:rs.ends.length,seconds:ss.reduce((n,x)=>n+x,0),sessions:sessions.length,avgSession:ss.length?Math.round(ss.reduce((a,b)=>a+b,0)/ss.length):0,lastPlayed:last?.at||null,favourite:fav.has(g.id),runStats:rs}});let days=[];try{days=parse(getRaw('sudsJackHubDays'))||[]}catch{}return{games,days:Array.isArray(days)?days:[],credits:Number(parse(getRaw('sudsJackHubCredits'))||0),tickets:Number(parse(getRaw('sudsJackHubTickets'))||0)}}
export function playedGames(){return snapshot().games.filter(g=>g.best!=null||g.visits>0||g.runs>0||g.failures>0||g.seconds>0||g.favourite)}
export function findGame(raw){const q=String(raw||'').toLowerCase();return GAMES.find(g=>q.includes(g.id.toLowerCase())||q.includes(String(g.title||'').toLowerCase()))||null}
export function favouriteGame(raw,on=true){const g=findGame(raw);if(!g)return null;setFavourite(g.id,on);return{id:g.id,title:label(g),on}}
function observation(g,all){const byTime=all.slice().sort((a,b)=>b.seconds-a.seconds),byVisits=all.slice().sort((a,b)=>b.visits-a.visits);if(g.seconds>0&&byTime[0]?.id===g.id&&all.filter(x=>x.seconds>0).length>1)return'YOU SPEND THE MOST TIME HERE.';if(g.visits>=3&&byVisits[0]?.id===g.id)return'YOU KEEP COMING BACK TO THIS ONE.';if(g.avgSession>=600)return'YOUR SESSIONS HERE ARE LONG. THIS IS NOT A DRIVE-BY GAME FOR YOU.';if(g.runStats.topCause?.[1]>=3)return`${String(g.runStats.topCause[0]).toUpperCase()} KEEPS ENDING YOUR RUNS. THAT IS A PATTERN NOW.`;if(g.failures>=3)return'YOU HAVE FAILED HERE ENOUGH TIMES FOR IT TO BE INFORMATION, NOT AN ACCIDENT.';if(g.favourite)return'YOU CALLED THIS ONE A FAVOURITE. I WILL HOLD YOU TO THAT.';return null}
export function patternLines(raw){const g0=findGame(raw),games=snapshot().games;if(g0){const g=games.find(x=>x.id===g0.id),r=g.runStats;if(!r.ends.length)return[`${g.title.toUpperCase()} HAS NO EXPLICIT RUN-END EVIDENCE YET.`,'I WILL NOT TURN PAGE CLOSES INTO DEATHS. THE GAME HAS TO REPORT THEM.'];const lines=[`${g.title.toUpperCase()} — ${r.ends.length} RECORDED RUN END${r.ends.length===1?'':'S'}.`];if(r.topCause)lines.push(`${String(r.topCause[0]).toUpperCase()} IS THE MOST COMMON END: ${r.topCause[1]} TIME${r.topCause[1]===1?'':'S'}.`);return lines}const wr=games.filter(x=>x.runStats.ends.length).sort((a,b)=>b.runStats.ends.length-a.runStats.ends.length);if(!wr.length)return['NO EXPLICIT RUN-END PATTERNS YET.','VISITS AND TIME ARE REAL, BUT THEY ARE NOT DEATHS. I AM WAITING FOR THE GAMES TO TELL ME THE DIFFERENCE.'];return wr.slice(0,4).map(g=>`${g.title.toUpperCase()}: ${g.runStats.ends.length} ENDS${g.runStats.topCause?` · MOST OFTEN ${String(g.runStats.topCause[0]).toUpperCase()}`:''}.`)}
export function reflectionLines(){const played=playedGames();if(!played.length)return['NOT ENOUGH EVIDENCE YET.','PLAY SOMETHING. I WOULD RATHER LEARN FROM BEHAVIOUR THAN ASK YOU TO FILL OUT A PROFILE.'];const byTime=played.filter(x=>x.seconds>0).sort((a,b)=>b.seconds-a.seconds),byReturn=played.slice().sort((a,b)=>b.visits-a.visits),fav=played.filter(x=>x.favourite),lines=['A FEW THINGS I NOTICE:'];if(byTime[0])lines.push(`${byTime[0].title.toUpperCase()} GETS THE MOST OF YOUR TIME${byTime[0].avgSession?` — ABOUT ${mins(byTime[0].avgSession)} A SESSION`:''}.`);if(byReturn[0]?.visits>=3)lines.push(`${byReturn[0].title.toUpperCase()} IS THE ONE YOU RETURN TO MOST.`);if(fav.length)lines.push(`YOU HAVE CALLED ${fav.map(x=>x.title.toUpperCase()).join(', ')} ${fav.length===1?'A FAVOURITE':'FAVOURITES'}. THAT IS WHAT YOU SAY. THE TIME LOG IS WHAT YOU DO.`);lines.push('THIS IS NOT A DIAGNOSIS. IT IS JUST A MIRROR MADE FROM BUTTON PRESSES.');return lines}
export function summaryLines(){const played=playedGames();if(!played.length)return['I CAN SEE THE CATALOGUE, BUT NOT ENOUGH PLAY HISTORY YET.','Scores already saved by the games will appear here. New sessions also leave coarse local visit and duration evidence.'];const ranked=played.slice().sort((a,b)=>(b.seconds/30+b.visits+b.runs+(b.best!=null?1:0))-(a.seconds/30+a.visits+a.runs+(a.best!=null?1:0))),lines=[`I HAVE SIGNAL FROM ${played.length} GAME${played.length===1?'':'S'} ON THIS DEVICE.`];for(const g of ranked.slice(0,5)){const bits=[];if(g.best!=null)bits.push(`BEST ${fmt(GAMES.find(x=>x.id===g.id),g.best)}`);if(g.visits)bits.push(`${g.visits} VISIT${g.visits===1?'':'S'}`);if(g.seconds)bits.push(`${mins(g.seconds)} PLAY`);if(g.failures)bits.push(`${g.failures} RUN END${g.failures===1?'':'S'}`);if(g.favourite)bits.push('FAVOURITE');lines.push(`${g.title.toUpperCase()} — ${bits.join(' · ')||'SEEN'}`)}const note=ranked[0]&&observation(ranked[0],played);if(note)lines.push(note);return lines}
export function gameLines(raw){const g0=findGame(raw);if(!g0)return null;const all=snapshot().games,g=all.find(x=>x.id===g0.id),lines=[g.title.toUpperCase()];if(g.best!=null)lines.push(`YOUR BEST HERE: ${fmt(g0,g.best)}.`);if(g.visits||g.runs||g.seconds)lines.push(`LOCAL LOG: ${g.visits} VISITS${g.runs?` · ${g.runs} RUNS`:''}${g.seconds?` · ${mins(g.seconds)} PLAY`:''}${g.avgSession?` · AVG ${mins(g.avgSession)}`:''}.`);if(g.failures)lines.push(`RUN ENDS REMEMBERED: ${g.failures}.`);if(g.runStats.topCause)lines.push(`MOST COMMON END: ${String(g.runStats.topCause[0]).toUpperCase()} ×${g.runStats.topCause[1]}.`);if(g.favourite)lines.push('YOU MARKED THIS ONE AS A FAVOURITE.');const note=observation(g,all.filter(x=>x.visits||x.seconds||x.best!=null||x.favourite));if(note)lines.push(note);if(g.lineage)lines.push(`ITS DECLARED LINEAGE: ${g.lineage}.`);return lines}
const api={snapshot,playedGames,findGame,favouriteGame,summaryLines,gameLines,reflectionLines,patternLines};globalThis.TokoPlayAwareness=api;export default api;
