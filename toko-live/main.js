import { mountChat } from '../toko/js/chat.js?v=20';
import { drawFace } from '../toko/js/face.js';
import { TOKO } from '../toko/js/palette.js';
const slot=document.querySelector('#chat-slot');
const chat=mountChat(slot,{where:'in',openOnLoad:true});window.__tokoLiveChat=chat;
await import('../toko/js/mind.js?v=10');await import('../toko/js/project-conversation.js?v=2');await import('../toko/js/brain-conversation.js?v=1');await import('../toko/js/conversation-plus.js?v=10');await import('../toko/js/chat-layout-fix.js?v=1');
const canvas=document.querySelector('#toko-stage'),ctx=canvas.getContext('2d'),stage=document.querySelector('.stage'),label=document.querySelector('#state-label'),thought=document.querySelector('#thought');
let state='listening',stateUntil=0,mx=0,my=0,gaze=0,lastUser='';
const STATES={listening:'LISTENING',thinking:'THINKING',talking:'TALKING',pleased:'PLEASED',glitch:'RECONSIDERING',memory:'REMEMBERING',project:'PROJECT MODE',news:'READING WIRE',mirror:'MIRROR'};
function setState(next,ms=1300,note=''){state=next;stateUntil=performance.now()+ms;stage.dataset.state=next;label.textContent=STATES[next]||next.toUpperCase();if(note)thought.textContent=note.slice(0,180).toUpperCase()}
addEventListener('pointermove',e=>{mx=(e.clientX/innerWidth-.5)*2;my=(e.clientY/innerHeight-.5)*2},{passive:true});
function rr(x,y,w,h,r,fill){ctx.beginPath();ctx.roundRect(x,y,w,h,r);ctx.fillStyle=fill;ctx.fill()}
function limb(x,y,len,ang,w=30){ctx.save();ctx.translate(x,y);ctx.rotate(ang);rr(-w/2,0,w,len,w*.45,'#0c0c10');ctx.fillStyle='#f0027f';ctx.beginPath();ctx.arc(0,len,Math.max(11,w*.42),0,Math.PI*2);ctx.fill();ctx.restore()}
function rings(cx,cy,t){if(state!=='memory'&&state!=='project'&&state!=='news')return;ctx.save();ctx.globalAlpha=.28;ctx.strokeStyle=state==='news'?'#e8e05a':'#f0027f';ctx.lineWidth=2;for(let i=0;i<3;i++){const r=145+((t*.03+i*52)%150);ctx.beginPath();ctx.arc(cx,cy,r,0,Math.PI*2);ctx.stroke()}ctx.restore()}
function body(t){const W=canvas.width,H=canvas.height;ctx.clearRect(0,0,W,H);const bob=Math.sin(t*.0017)*5+(state==='talking'?Math.sin(t*.025)*2:0),breathe=Math.sin(t*.0012)*4;gaze+=(mx-gaze)*.06;const cx=W*.5+gaze*13,cy=H*.37+my*6+bob;const grad=ctx.createRadialGradient(cx,H*.79,10,cx,H*.79,250);grad.addColorStop(0,state==='mirror'?'rgba(255,255,255,.12)':'rgba(240,2,127,.20)');grad.addColorStop(1,'rgba(240,2,127,0)');ctx.fillStyle=grad;ctx.fillRect(0,H*.45,W,H*.55);rings(cx,cy,t);
  ctx.save();ctx.translate(cx,H*.53+breathe);ctx.fillStyle='#101015';ctx.beginPath();ctx.moveTo(-138,58);ctx.quadraticCurveTo(-120,-40,-72,-58);ctx.lineTo(72,-58);ctx.quadraticCurveTo(120,-40,138,58);ctx.lineTo(106,188);ctx.lineTo(-106,188);ctx.closePath();ctx.fill();ctx.strokeStyle='#2c1727';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(0,-52);ctx.lineTo(0,178);ctx.stroke();
  let la=.05+Math.sin(t*.004)*.03,ra=-.05-Math.sin(t*.004)*.03;if(state==='thinking'){la=-.65;ra=.2}if(state==='talking'){la=-.35+Math.sin(t*.012)*.22;ra=.38+Math.sin(t*.01+1)*.2}if(state==='pleased'){la=-.75;ra=.75}if(state==='glitch'){la=.55;ra=-.55}if(state==='memory'){la=-1.0;ra=.1}if(state==='project'){la=-.2;ra=.95}if(state==='news'){la=.28;ra=-.9}if(state==='mirror'){la=-1.1;ra=1.1}limb(-108,28,128,la);limb(108,28,128,ra);ctx.restore();
  ctx.save();ctx.translate(cx-118,cy-118);if(state==='glitch'){ctx.translate(Math.sin(t*.08)*5,0)}ctx.fillStyle=TOKO.MAGENTA||'#f0027f';ctx.beginPath();ctx.arc(118,118,112,0,Math.PI*2);ctx.fill();ctx.fillStyle='#050507';ctx.beginPath();ctx.arc(118,118,94,0,Math.PI*2);ctx.fill();let open=.08;const blink=t%5200;if(blink>5050)open=.95;if(state==='pleased')open=.30;if(state==='thinking')open=.02;if(state==='glitch')open=.72;if(state==='memory')open=.15;if(state==='project')open=.06;if(state==='news')open=.12;if(state==='mirror')open=.92;drawFace(ctx,18,18,200,{color:'#fff',open});ctx.restore();
  if(state==='mirror'){ctx.save();ctx.globalAlpha=.18;ctx.scale(-1,1);ctx.drawImage(canvas,-W,0);ctx.restore()}
  ctx.font='12px "Courier New",monospace';ctx.fillStyle='rgba(240,2,127,.72)';const glyph=state==='thinking'?'RETRIEVE > RANK > CONNECT':state==='glitch'?'CORRECTION > OLD NOTE':state==='memory'?'MEMORY > RECALL > WEIGHT':state==='project'?'DESIGN > ROADMAP > LOCKS':state==='news'?'WIRE > SOURCE > OPINION':state==='mirror'?'PLAYER <> SOFTWARE <> AUTHOR':'LOCAL / MEMORY / DESIGN';ctx.fillText(glyph,24,H-26)}
function loop(t){if(stateUntil&&t>stateUntil){state='listening';stage.dataset.state='listening';label.textContent='LISTENING';stateUntil=0}body(t);requestAnimationFrame(loop)}requestAnimationFrame(loop);
function stateFor(text,who){const s=text.toLowerCase();if(/mirror\.exe|\bmirror\b/.test(s))return['mirror',3600,'THE ROOM IS LOOKING BACK.'];if(/memory|remember|learned|correction/.test(s))return['memory',2200,'SEARCHING LOCAL MEMORY AND CORRECTIONS.'];if(/news|headline|story|wire|industry/.test(s))return['news',2200,'READING SOURCES BEFORE OPINION.'];if(/roadmap|design|project|tiny hawk|eeri|piritori|hyper dagger|suds jack|flash prince|betterment|toko drop/.test(s))return['project',2200,'PROJECT CONTEXT LOCKED.'];if(who==='you'&&/^(no|actually|correction|you are wrong|that is wrong|not quite)/i.test(text))return['glitch',1500,'CORRECTION RECEIVED. RE-WEIGHTING MEMORY.'];if(who==='you')return['thinking',950,'LOOKING FOR RELATED MEMORY, DESIGN AND PROJECT EVIDENCE.'];return[/good|yes|right|favourite|beaut|fun|thank/i.test(text)?'pleased':'talking',Math.min(3000,1000+text.length*8),text]}
const log=document.querySelector('.toko-chat .tc-log');if(log)new MutationObserver(muts=>{for(const n of muts.flatMap(m=>[...m.addedNodes]).filter(n=>n.nodeType===1)){const text=(n.textContent||'').trim();if(!text)continue;const who=n.classList.contains('tc-you')?'you':n.classList.contains('tc-me')?'me':null;if(!who)continue;if(who==='you')lastUser=text;const [s,ms,note]=stateFor(text,who);setState(s,ms,note)}}).observe(log,{childList:true,subtree:true});
document.querySelectorAll('[data-say]').forEach(b=>b.addEventListener('click',()=>{const input=document.querySelector('.toko-chat .tc-say-row input');if(!input)return;input.value=b.dataset.say;input.focus();input.dispatchEvent(new KeyboardEvent('keydown',{key:'Enter',bubbles:true,cancelable:true}))}));
