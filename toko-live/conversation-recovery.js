// Toko Live v24 — never dead-end on an uncertain phrase.
// Runs after the main response engine. If no other handler consumed Enter,
// it offers concrete interpretations based on current thread instead of "I don't know".
const PROJECTS=['Tiny Hawk','Eeri','Piritori','Betterment','Hyper Dagger','Suds Jack','Flash Prince','Toko Drop'];
const clean=s=>String(s||'').replace(/\s+/g,' ').trim();
function current(){const c=window.TokoLiveContext?.snapshot?.()||{};return {project:c.project||null,subject:c.subject||null,lastToko:c.lastToko||'',lastYou:c.lastYou||''}}
function guesses(raw,c){const q=raw.toLowerCase(),g=[];if(c.project){g.push(`Do you mean the biggest problem in ${c.project}?`,`Do you mean what we should change next in ${c.project}?`,`Or do you want the reasoning behind the last point?`)}else if(c.subject){g.push(`Do you mean ${c.subject}?`,`Do you want me to critique that?`,`Or should I connect it to a current project?`)}else{const p=PROJECTS.find(x=>q.includes(x.toLowerCase().split(' ')[0]));if(p)g.push(`Do you mean ${p}?`);g.push('Do you want a critique?','Do you want the next action?','Or are you asking for my opinion?')}return [...new Set(g)].slice(0,3)}
function append(raw,lines){const log=document.querySelector('.toko-chat .tc-log');if(!log)return;const you=document.createElement('p');you.className='tc-you';you.textContent=raw.toUpperCase();const me=document.createElement('p');me.className='tc-me';me.textContent=`I am not certain which part you mean. I can still follow the thread. ${lines.join(' ')}`;log.append(you,me);log.scrollTop=log.scrollHeight;dispatchEvent(new CustomEvent('toko:recovery',{detail:{raw,guesses:lines}}))}
function boot(){const i=document.querySelector('.toko-chat .tc-say-row input');if(!i)return requestAnimationFrame(boot);i.addEventListener('keydown',e=>{if(e.key!=='Enter'||e.defaultPrevented)return;const raw=clean(i.value);if(!raw)return;setTimeout(()=>{if(!i.value||clean(i.value)!==raw)return;const c=current(),gs=guesses(raw,c);i.value='';append(raw,gs)},0)},false)}boot();
